<div>
    <p>右端のボタンで貸出状態を変更できます。</p>
</div>
<table class="table table-striped table-hover">
    <thead>
        <tr>
            <td></td>
            <td>タイトル</td>
            <td></td>
            <td></td>
            <td>状態</td>
        </tr>
    </thead>
    <tbody>
        <% @books.each do |book| %>
        <tr id="book_<%= book.id %>">
            <td><%= link_to image_tag(book.medium_image, :width => "50"), book.detail_page_url %></td>
            <td><%= link_to book.title, book_path(book) %></td>
            <td><%= link_to "詳細", book_path(book) %></td>
            <td><%= link_to "amazon", book.detail_page_url%></td>
            <td><a class="btn btn-primary">貸出中</a></td>
        </tr>
        <% end %>
    </tbody>
</table>
