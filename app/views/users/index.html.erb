<h2>アカウント新規作成</h2>
<div>
    <form action="users/search" method="post">
        <fieldset>
            <p>あなたの名前、プロフィール画像URLを取得します。<br />Twitterアカウント IDを入力してください。</p>
            <input type="text" name="screen_name" placeholder="例: sugimori" class="input-medium search-query">
            <input type="submit" value="<%= $button_submit %>" class="btn">
        </fieldset>
    </form>
</div>

<h2>アカウント一覧</h2>
<table class="table table-striped table-hover">
    <thead>
        <tr>
            <td class="center"></td>
            <td class="center"><%= $user_screen_name %></td>
            <td class="center"><%= $user_name %></td>
            <td class="center"></td>
            <td class="center"></td>
        </tr>
    </thead>
    <tbody>
        <% @users.each do |user| %>
        <tr id="user_<%= user.id %>">
            <td class="center"><%= link_to image_tag(user.profile_image_url, :width => $twitter_image_width), "https://twitter.com/" + user.screen_name %></td>
            <td class="center"><%= link_to user.screen_name, user_path(user) %></td>
            <td class="center"><%= link_to user.name, user_path(user) %></td>
            <td class="center"><%= link_to $button_edit, edit_user_path(user), :class => "btn btn-warning btn-large" %></td>
            <td class="center"><%= link_to $button_delete, user, :confirm => '本当に削除しますか？', :method => :delete, :class => "btn btn-danger btn-large" %></td>
        </tr>
        <% end %>
    </tbody>
</table>